# Public Mapbox token used by the mobile app.
EXPO_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.replace_me

# Firebase native app config is supplied through:
# apps/mobile/google-services.json
# apps/mobile/GoogleService-Info.plist
#
# Keep environment-specific files out of source control until the Firebase
# project is intentionally configured for this public portfolio repository.
