{
  "rules": {
    "presence": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['geohash','activityType','joinable','updatedAt'])"
      }
    },

    "liveSessions": {
      "$sessionId": {
        ".read": "auth != null",
        ".write": "auth != null && (((!data.exists() && newData.child('ownerId').val() === auth.uid) || (data.exists() && data.child('ownerId').val() === auth.uid)) && (!newData.exists() || newData.child('visibility').val() === 'private' || auth.token.firebase.sign_in_provider !== 'anonymous'))",
        ".validate": "!newData.exists() || newData.hasChildren(['ownerId','visibility','activityType','startedAt'])"
      }
    },

    "coarseSessionLocation": {
      "$sessionId": {
        ".read": "auth != null",
        ".write": "auth != null && root.child('liveSessions').child($sessionId).child('ownerId').val() === auth.uid",
        ".validate": "!newData.exists() || newData.hasChildren(['latitude','longitude','updatedAt'])"
      }
    },

    "sessionMembers": {
      "$sessionId": {
        ".read": "auth != null && (root.child('liveSessions').child($sessionId).child('ownerId').val() === auth.uid || data.child(auth.uid).val() === true)",
        ".write": false
      }
    },

    "preciseSessionLocation": {
      "$sessionId": {
        ".read": "auth != null && (root.child('liveSessions').child($sessionId).child('ownerId').val() === auth.uid || root.child('sessionMembers').child($sessionId).child(auth.uid).val() === true)",
        "$uid": {
          ".write": "auth != null && auth.uid === $uid && (root.child('liveSessions').child($sessionId).child('ownerId').val() === auth.uid || root.child('sessionMembers').child($sessionId).child(auth.uid).val() === true)",
          ".validate": "!newData.exists() || newData.hasChildren(['latitude','longitude','updatedAt'])"
        }
      }
    }
  }
}
